import 'package:flutter/material.dart';

class ServiciosSubsidiaryPage extends StatelessWidget {
  const ServiciosSubsidiaryPage({Key? key, required this.idSubsidiary}) : super(key: key);
  final String idSubsidiary;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
